{
  "name": "graphql-companies",
  "version": "1.0.0",
  "description": "A graphql companies app. Learning purposes.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm install && cd client && npm install && npm run build",
    "start": "node server.js"
  },
  "keywords": [
    "graphql"
  ],
  "author": "lb",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "express": "^4.17.1",
    "express-graphql": "^0.11.0",
    "graphql": "^15.4.0"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "cors": "^2.8.5",
    "nodemon": "^2.0.6"
  }
}